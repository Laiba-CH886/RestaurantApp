plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
    id "com.google.gms.google-services"

}

android {
    namespace = "com.example.kkgfrontend"
    compileSdk = 34  // ✅ Updated to SDK version 34

    defaultConfig {
        applicationId = "com.example.kkgfrontend"
        minSdkVersion 23
        targetSdk = 33 // ✅ Updated to SDK version 33
        versionCode = 1
        versionName = "1.0"
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    buildFeatures {
        buildConfig = true  // ✅ This is the FIX for your error
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation platform('com.google.firebase:firebase-bom:32.2.0') // Use latest version
    implementation 'com.google.firebase:firebase-auth'
    implementation 'com.google.firebase:firebase-firestore'
    implementation 'com.google.android.gms:play-services-auth:20.5.0'  // Ensure you're using the latest version
}

apply plugin: 'com.android.application'
apply plugin: 'com.google.gms.google-services'

